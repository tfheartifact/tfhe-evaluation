# Parameter Inputs

`seta_unix_parameters.json` records the SetA to UniX parameter pairs used by the
correctness and security-validation code. The Rust correctness examples also
contain the same built-in suite so the scripts can run without an explicit
parameter file.
